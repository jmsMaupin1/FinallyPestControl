{
  "name": "finallypestcontrol",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "gulp-plumber": "^1.1.0",
    "react-scripts": "0.8.5"
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "scripts": {
    "deploy": "npm run build && firebase deploy",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
